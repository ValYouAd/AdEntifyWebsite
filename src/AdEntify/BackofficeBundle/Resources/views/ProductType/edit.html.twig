{% extends '::base.admin.html.twig' %}

{% block body -%}
    <div clas="page-header">
        <h1><i class="glyphicon glyphicon-edit"></i> {{ 'Product Type edit'|trans }}</h1>
    </div>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('producttype') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

{% extends '::base.admin.html.twig' %}

{% block body -%}
    <h1>{{ 'Brand edit'|trans }}</h1>

    {{ form(edit_form) }}

    <a class="btn" href="{{ path('brands') }}">
        {{ 'Back to the list'|trans }}
    </a>
    {{ form(delete_form) }}
{% endblock %}

{% extends '::base.admin.html.twig' %}

{% block body -%}
    <h1>Category edit</h1>

    {{ form(edit_form, {'attr': {'class' : 'form-horizontal'} }) }}

    <a class="btn btn-default" href="{{ path('categories') }}">
        {{ 'Back to the list'|trans }}
    </a>
    {{ form(delete_form) }}
{% endblock %}

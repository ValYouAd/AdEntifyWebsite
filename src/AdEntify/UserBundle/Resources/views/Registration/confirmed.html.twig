{% extends "::base.html.twig" %}

{% block body %}
    {% block fos_user_content %}
        <p>{{ 'registration.confirmed'|trans({}, 'FOSUserBundle') }}</p>
        <p>{{ 'Please wait...'|trans() }}</p>
        <meta http-equiv="refresh" content="3;URL='{{ path('loggedInHome', { '_locale': app.request.locale }) }}'">
    {% endblock fos_user_content %}
{% endblock %}